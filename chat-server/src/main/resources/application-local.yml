spring:
  datasource:
    url: jdbc:mysql://localhost:3306/talk?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: none
  data:
    redis:
      host: localhost
      port: 6379
  session:
    redis:
      namespace: spring:session
