server:
  port: 8081
  instanceId: 1
  servlet:
    session:
      cookie:
        name: JSESSIONID

spring:
  application:
    name: chat-server
  data:
    redis:
      host: localhost
      port: 6379
  session:
    redis:
      namespace: spring:session
